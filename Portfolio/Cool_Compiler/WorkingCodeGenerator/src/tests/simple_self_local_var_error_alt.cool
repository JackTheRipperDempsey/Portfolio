
class Adder {
    bool : Bool <- true;

};

class Intermediate {

    falseBool : Bool <- false;


	adder : Adder;

    returnsFalse() : Bool {
        {falseBool;}
    };

    intermediateAdder (intermediate : Intermediate, flag : Bool) : Int {
    {
        0;
    }
    };

};



class Main inherits IO {
    number : Int := 0;

    adder : Intermediate;
    flag : Bool := true;


    main() : Main {
        {
            (let self : String in
                {
                    self <- "x";
                    out_string(self);
                }
            );
	        number := adder.intermediateAdder(adder, flag);
            out_string("incrementing complete");
        
	    }
    };

};
